#include "main.h"

/**
 *_sqrt_recursion - evaluate function sqrt
 *@i: imteger
 *@n: integer
 *Return: evaluate sqrt
 */
int _sqrt_recursion(int n)
{
  if (n == 0 || n == 1)
    {
      return (n);
    }
  else if (n < 0)
    {
      return (-1);
    }
}
